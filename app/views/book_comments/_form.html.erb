<%= form_with model: [book, book_comment], local: false do |f| %>
  <%= f.text_area :comment, rows: '5', class: 'form_text_area', placeholder: "コメントをここに" %>
<div>
  <%= f.submit "送信する" %>
</div>
<% end %>
